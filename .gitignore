# ---- General OS & Editor Junk ----
# macOS / Windows / Linux
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Temporary files
*.tmp
*.swp
*.swo
*~

# Logs
*.log

# Node / Package Manager Artifacts (if any appear later)
node_modules/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python (if utility scripts get added)
__pycache__/
*.py[cod]
*.pyo

# Archives / backups
*.zip
*.tar
*.tar.gz
*.7z
*.bak

# Editors / IDEs
.vscode/
.idea/
*.code-workspace

# Git merge / patch leftovers
*.orig
*.rej

# Scoop cache and auto-generated config
scoop/cache/
scoop/config.json

# Neovim transient data (keep config + lazy-lock for reproducibility)
**/nvim/undodir/
**/nvim/session*/
**/nvim/.tests/

# WezTerm local state (keep wezterm.lua)
wezterm/.cache/

# Komorebi generated logs (keep JSON configs)
komorebi/*.log

# Android emulator artifacts (local only)
.android/

# Update notifier / telemetry (ignore contents)
configstore/**
!configstore/.gitkeep

# Copilot / symbol / cache databases
**/*.db
**/*.db-*

# GitHub Copilot generated cache/state (not useful across devices)
github-copilot/

# Lock files usually not needed unless you want deterministic plugin fetch
package-lock.json
pnpm-lock.yaml
yarn.lock

# Allowlist: explicitly keep important tracked files
!nvim/lazy-lock.json
# Ignore specific Komorebi layout config
komorebi/komorebi.json
!wezterm/wezterm.lua
!whkdrc

# End of file